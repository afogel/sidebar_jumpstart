<!DOCTYPE html>
<html class='h-full'>
  <head>
    <%= render 'shared/head' %>
  </head>

  <body class='bg-base-100 drawer lg:drawer-open h-full'>
    <div class="drawer-content overflow-scroll">
      <div class="max-w-[100vw] px-6 xl:pr-2 h-[94vh] ">
        <div class="mx-auto max-w-7xl pt-16 lg:flex lg:gap-x-16 lg:px-8 flex flex-col h-full">
          <% if current_user != true_user %>
            <div class="alert alert-warning text-center">
              You're logged in as <b><%= current_user.name %> (<%= current_user.email %>)</b>
              <%= link_to stop_impersonating_madmin_impersonates_path, method: :post do %><%= icon("fas", "times") %> Logout <% end %>
            </div>
          <% end %>
          <%= render 'shared/notices' %>
          <%= yield %>
          <%= render 'shared/footer' %>
        </div>
      </div>
    </div>
    <%= render 'shared/navbar' %>
  </body>
</html>
